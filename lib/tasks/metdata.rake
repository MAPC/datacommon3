namespace :metadata do
  desc "Download all Weave visualizations as preview images"
  task :update => :environment do

    # select_metadata_tables = "SELECT name FROM metadata._public_tables"

    # tables = ActiveRecord::Base.connection.execute select_metadata_tables
    
    # tables.each_row do |table|
      
    # end

  end
end