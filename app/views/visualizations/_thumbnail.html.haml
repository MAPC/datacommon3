.col-md-3.col-sm-6.col-xs-12.visual.thumbnail
  = link_to visual do
    = image_tag visual.image.url(:gallery), alt: visual.title
    - if visual.private?
      %span.badge Private
    %p.visual.title= visual.title
  %p.byline
    by
    = link_to visual.owner, visual.owner