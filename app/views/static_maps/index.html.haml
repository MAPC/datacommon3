%h1 Regional Map Gallery
%h2.page-header Explore the maps MAPC produces to communicate the state of the region.

%p= @institution.branding.map_gallery_intro

= render @maps

.row.center
  = paginate @maps, window: 1, outer_window: 2